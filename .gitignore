# Build outputs
build/
target/
out/
*.apk
*.aab
*.ap_
*.dex
*.class

# Gradle
.gradle/
!gradle/wrapper/gradle-wrapper.jar
local.properties

# Rust
**/target/
Cargo.lock.orig

# IDE
.idea/
*.iml
.vscode/
.DS_Store
Thumbs.db

# Secrets and local state — never commit
*.keystore
*.jks
*.pem
*.p12
*.env
.env*
secrets/
**/local.secrets.*
# Entitlement operator material (Phase 8 runbooks K1/K3): Tor onion service secret keys
# (ghost-issuer-ops onion-keygen), key load files, the custody secret and the schedule key live
# outside the repository (%USERPROFILE%\.ghost-stagenet-keys\ for stagenet).
hs_ed25519_secret_key
*.ghkl
custody.secret
schedule.key

# Temporary
*.log
*.tmp
.temp/
.kotlin/
build-repro/
**/jniLibs/
ghost/native-libs.sha256
.claude/worktrees/
