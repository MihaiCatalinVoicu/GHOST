# Invarianți de confidențialitate (T1–T21)

Fiecare invariant are: ce afirmă, cum se verifică, când devine executabil, și unde trăiește codul. Un invariant fără test executabil nu poate susține o afirmație în UI (CP-10).

| ID | Invariant | Verificare | Executabil din | Cod |
|---|---|---|---|---|
| T1 | Un relay observă doar câmpurile din `allowed-observables.json`; niciodată IP, identitate, plaintext, dimensiune nepadată, timp sub minut | relay-ul în mod captură scrie NDJSON; `capture-check` validează fiecare linie | **Faza 2** (validator + fixture-uri); **Faza 5** (captură reală din suita cu două noduri, `relay/crates/node/tests`) | `capture-check/` |
| T2 | Jurnalul issuer-ului și nullifier-ele relay-urilor nu au nicio cheie de join | test de integrare: se rulează N plăți sintetice, se exportă ambele jurnale, se caută orice valoare comună sau corelare peste prag statistic | Faza 8 | `issuer/` tests |
| T3 | Secretele-canar nu apar în log/telemetrie/crash | se injectează șiruri unice (seed, chei, capabilități, ID-uri) și se grep-uiește tot ce iese din proces | Faza 3 | `android/` instrumentation |
| T4 | Backup/device-transfer nu conțin seed, cheie DB, stare protocol, cache decriptat | `bmgr`/`adb backup` pe emulator, inspecție arhivă | Faza 4 | `android/` instrumentation |
| T5 | Media criptată nu mai conține EXIF/XMP/GPS/atomi de locație | fișiere cu metadate cunoscute → decriptare → parser de metadate returnează gol | Faza 11 | `android/media` tests |
| T6 | Fără Tor: zero conexiuni clearnet și zero interogări DNS | în `client-core/net` din Faza 6: singura destinație publică e `OnionAddress` (checksum v3; vectori partajați Rust + Kotlin), clientul Arti nu e expus (doctest `compile_fail`), tonic e legat doar cu `codegen` (fără `Channel`/`Endpoint`, verificat de `rust-feature-policy.sh`), iar în crate clippy interzice o listă de API-uri clearnet și DNS (socket-uri std/tokio, rezolvatoare, conectorul HTTP hyper-util, TCP/UDP brut al runtime-ului) și orice apel de conectare Arti în afara `connect_isolated`; un fixture negativ dovedește că fiecare interdicție se declanșează. Din Faza 7, partea Kotlin: `kotlin-clearnet.sh` interzice în orice sursă de producție `android/*/src/main` (Kotlin și Java) primitivele clearnet ale JVM: socket-uri, URL-uri și conexiuni URL, rezolvare de nume și adrese `java.net`, proxy-uri, clientul `java.net.http`, importul wildcard `java.net.*`, `URI.toURL()` și orice din `javax.net`; fixture negativ cu 12 linii marcate (`test-harness/gates/negative-clearnet/`). **Nu acoperă** un API care nu e pe una din liste (de exemplu alte API-uri de rețea ale platformei Android sau acces prin reflecție) ori alt crate (allowlist-ul și `deny.toml` țin afară doar pachete nerevizuite); acoperirea completă vine de la testul dinamic: emulator cu Tor blocat + captură `tcpdump`, 0 pachete non-Tor | **Faza 6** (API + gate-uri Rust); **Faza 7** (gate Kotlin); Faza 13 (emulator) | `client-core/net`, `android/network` tests, `scripts/gates/rust-client-allowlist.sh`, `scripts/gates/kotlin-clearnet.sh` |
| T7 | Orice dependență în afara allowlist-ului oprește build-ul | Gradle: `dependency-allowlist.sh` + fixture negativ (Faza 1); Rust (biblioteca nativă, ambele ABI): `rust-client-allowlist.sh` + self-test (Faza 6); potrivire după nume de pachet | **Faza 1**, extins în Faza 6 | `scripts/gates/` |
| T8 | Niciun placeholder pe calea de producție | `anti-placeholder.sh` + fixture negativ | **Faza 1** | `scripts/gates/` |
| T9 | Pe fir și în stocare apar doar dimensiuni din {1,4,16,64} KiB, cu octeți indistinctibili de aleator | relay-ul refuză orice altă dimensiune (T1, `size_bucket`); clientul refuză să trimită blob-uri care nu sunt bucket (`not_bucket_sized`); padding-ul se aplică în plaintext-ul AEAD (`pad_plaintext`), deci fără prefix de lungime în clar; captură rețea | Faza 5/6 (dimensiuni); Faza 9/10 (criptare + padding în AEAD) | `capture-check/`, `relay/crates/transport`, `client-core/net` tests |
| T10 | Ecranele sensibile au FLAG_SECURE; nimic în recents | test UI: screenshot returnează negru | Faza 13 | `android/app` UI tests |
| T11 | Notificările cu dispozitiv blocat nu conțin text/expeditor | test UI pe notificare | Faza 13 | `android/app` UI tests |
| T12 | Două build-uri release au hash identic | `reproducible-build.sh` (APK) + `apk-native-libs.sh` (APK-ul conține exact bibliotecile native înregistrate, pe ambele ABI; self-test pe arhive fabricate); bibliotecile native: două build-uri în aceeași cale, identice (CI). Identitatea între medii diferite (alt host, Windows) nu e încă dovedită: builder cu căi fixe în Faza 14 | **Faza 1**, extins în Faza 6 | `scripts/gates/` |
| T13 | Timestamps de autor au secunde = 0 | test unitar pe envelope | Faza 10 | `android/messaging` tests |
| T14 | Pseudonimele aceluiași utilizator în două canale nu sunt egale și nu sunt derivabile fără seed | vectori de derivare | Faza 3 | `android/identity` tests |
| T15 | Manifestele rămân întărite (allowBackup=false etc.); din Faza 7, și manifestul fuzionat de release: doar permisiuni din allowlist, nicio componentă exportată în afara activității LAUNCHER | `manifest-lint.sh` pe manifestele sursă + fixture negativ. Din Faza 7, `merged-manifest-lint.sh` (T15m) pe manifestul fuzionat de release al `:app`, rulat în jobul CI android după build: permisiunile din allowlist-ul T15 comun (`scripts/gates/common.sh`; `RECEIVE_BOOT_COMPLETED` și `ACCESS_NETWORK_STATE` prin ADR-20) plus permisiunea de semnătură `<applicationId>.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION`, singura pe care aplicația o poate declara; nicio componentă exportată în afara activității LAUNCHER (lista de excepții e goală după eliminarea `ProfileInstallReceiver`, ADR-20); o componentă cu intent-filter declară `exported` explicit; `SyncJobService` prezent, neexportat și protejat de `BIND_JOB_SERVICE`; `allowBackup="false"`, fără cleartext, nu debuggable. Fixture negativ cu 15 încălcări și fixture pozitiv (manifestul fuzionat al Fazei 7) în `test-harness/gates/negative-merged-manifest/` | **Faza 1**; T15m din **Faza 7** | `scripts/gates/` |
| T16 | Invitația nu conține host web, cheie de identitate în clar, și nu e rejucabilă | teste parser + redeem | Faza 3/8 | `android/identity`, `issuer/` |
| T17 | Traficul în idle e statistic indistinct de traficul activ (polling constant) | captură rețea pe două sesiuni; test KS pe distribuția intervalelor și dimensiunilor | Faza 12 (mutat din Faza 7 prin ADR-20; vezi nota de mai jos) | harness rețea |
| T18 | Cu bridges active, captura nu conține fingerprint Tor cunoscut | captură + set de semnături | Faza 12 | harness rețea |
| T19 | Programul de citire e independent de activitate: cu cheile, setul de perechi (relay, namespace) de citire, starea capabilităților și urma de latență a listărilor fixate, momentul de start, perechea și `limit` ale primei cereri de listare a fiecărui eveniment sunt identice cu și fără activitate în outbox/inbox (inclusiv tranzacții de consumator și eșecuri pe apelurile benzii de lucru). În high-privacy mode întreaga secvență de listări e identică, iar fiecare store și check cade pe un eveniment al perechii, la sau după `not_before`. Enqueue și consumul nu creează evenimente de citire | teste JVM deterministe, cu egalitate exactă și latență nenulă: `engine/T19BasicTest` (8 perechi pe 4 workeri, 10 % eșecuri de listare, 100 % eșecuri pe banda de lucru) și `harness/PrivacyHarnessTest` (standard: primele pagini identice; high-privacy: secvența completă; adăugarea sau scoaterea altor perechi nu schimbă programul unei perechi; `WakeScheduler` nu e accesibil din enqueue sau consum). O pagină în plus care rulează până la termen nu întârzie și nu sare prima pagină a altei perechi (`engine/ReadLaneTest`). Mutanții `SharedTick` (M13), `WorkFailureFeedsReadBreaker` (M15), `WorkPauseSuppressesLists` (M16) și `ContinuationsUseEventWorkers` (M17) sunt detectați de `harness/MutantDetectionTest`. Nu spune nimic despre volum sau despre captura de rețea (T17) | **Faza 7** (JVM) | `android/sync/src/test/.../engine/T19BasicTest.kt`, `.../harness/PrivacyHarnessTest.kt` |
| T20 | Stratul de sync nu scrie nimic în afara SQLCipher: niciun fișier, nicio preferință, niciun extra de job (statisticile de rețea și baterie pe care le ține sistemul de operare nu sunt scrise de stratul de sync și nu intră în T20; LIMITE L1.1). Fiecare coloană de timp persistată în tabelele de sync e `*_minute`, `*_hour` sau `*_day` cu CHECK potrivit (`retain_until_day` multiplu de 7 zile). Nu se persistă istoric de cereri, programe, breakere sau latențe; starea de reîncercare a lucrului în curs există doar cât lucrul e în curs (LIMITE L1.1). Rândurile `done` țin doar (namespace, hash, `retain_until_day`). Planificatorul ține un singur job periodic fără extras | JVM: `store/SchemaIntrospectionTest` (setul exact de 11 coloane de timp cu CHECK-urile lor, nicio altă coloană cu nume de timp, `retain_until_day % 7 = 0`, pe un rând `done` fiecare altă coloană refuzată de CHECK, `inbox_blob` WITHOUT ROWID) și `android/PeriodicJobSpecTest` (jobul dorit are extras și extras tranzitorii goale; toate cele 15 câmpuri comparate). Static: sursele `android/sync/src/main` nu folosesc SharedPreferences; singurul director de fișiere e cel al stării Arti (`noBackupFilesDir/tor`, Faza 6), care nu e stare de sync. Pe dispozitiv (manual până la CI pe emulator, Faza 13): inventarul sandbox-ului și `getAllPendingJobs()`, gol după wipe | **Faza 7** (JVM); dispozitiv: manual | `android/sync/src/test/.../store/SchemaIntrospectionTest.kt`, `.../android/PeriodicJobSpecTest.kt` |
| T21 | Scopul de izolare = scopul capabilității: fiecare apel către relay folosește izolarea de circuit a namespace-ului numit în antetul capabilității, iar tipul capabilității se potrivește operației (write pentru store; read sau write pentru get, list, check). O nepotrivire sau un token neparsabil eșuează cu `invalid_argument` înainte de orice I/O | teste Rust pe `NamespaceClient`: matricea token × operație; `a_mismatched_capability_never_reaches_the_relay`: un relay care numără cererile primește 0 cereri și conectorul deschide 0 conexiuni pentru fiecare token refuzat (8 cazuri × 4 operații, plus o capabilitate read la store), iar rularea de control face 5 cereri; `RelayClient` fără constructor public (doctest `compile_fail`) | **Faza 7** (`cargo test`, jobul CI rust) | `client-core/net/src/namespace_client.rs`, `client-core/net/src/lib.rs` |

## Nota T17 (Faza 7, ADR-20)

T17 se mută explicit în Faza 12 și poate fi atins numai în high-privacy mode, cu cover traffic (P2). În modul standard, trimiterile pleacă imediat ce sunt scadente, deci volumul de upload urmează activitatea; în ambele moduri, get-urile urmează scrierile altora, deci volumul de download urmează traficul primit, până la cover traffic. Faza 7 livrează doar precondiția JVM, T19: momentele listărilor nu depind de activitate.

## Formatul capturii de relay (T1)

O linie JSON per eveniment observabil. Câmpuri permise și forme: `allowed-observables.json`. Exemple: `fixtures/capture-ok.ndjson` (trece), `fixtures/capture-bad.ndjson` (fiecare linie încalcă altă regulă). Rulare:

```bash
cd ghost
cargo run -p ghost-capture-check -- --schema test-harness/privacy/allowed-observables.json --capture test-harness/privacy/fixtures/capture-ok.ndjson
```

Regula de evoluție: adăugarea unui câmp în schema permisă este o **decizie de confidențialitate** și cere un ADR sau o modificare a threat model-ului §6, nu doar un PR.
