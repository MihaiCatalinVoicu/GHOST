* text=auto eol=lf
*.sh text eol=lf
gradlew text eol=lf
*.bat text eol=crlf
*.jar binary
*.png binary
*.pdf binary
*.apk binary
# Entitlement Schedules (Phase 8): signed binary documents, hashed byte for byte.
*.ghes binary
# Sealed key files and key load files of the issuer (Phase 8): binary; committed as test fixtures only.
*.ghks binary
*.ghkl binary
