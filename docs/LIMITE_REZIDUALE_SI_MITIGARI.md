# Limitele reziduale și mitigările lor

| Câmp | Valoare |
|---|---|
| Status | Analiză + ADR-13…ADR-16 (**aprobate** 2026-09-10); ADR-20 (aprobat 2026-09-12); ADR-22 … ADR-26 (**propuse** 2026-09-12, aplicate în Faza 8, în așteptarea deciziei proprietarului) |
| Completează | `GHOST_Master_Plan_v2.1_OPTIMIZAT.md` §7 |
| Data | 2026-09-10 |
| Completări | 2026-09-11, Faza 7 (ADR-20): L1.1 (reziduuri locale ale sincronizării), L2.1 (reziduuri de trafic și limita de capacitate), conflictul din L4 (bootstrap-uri Tor periodice), trei rânduri noi în L5, sinteza din §5. 2026-09-13, Faza 8 (ADR-22 … ADR-26; design `docs/design/faza8-issuer.md` §12.6, §19 și Anexa B): L1.1 (reziduuri locale ale entitlement-ului), L2.1 punctul 2 (decizia D11), rânduri noi în L5, secțiunea nouă L6 (reziduurile E1–E29 ale entitlement-ului), sinteza din §5; după review-ul documentelor: E11 restrâns la ce e implementat, E28 și E29, trimiterile L6 la lumea T2 marcate ca lucru al slice-ului S10, două rânduri noi în L5; după valul E (design §19.24, §19.25): în L5 închise rândurile celor trei goluri de operare, al Q29 și al Q30 (cu ce rămâne din ele), actualizat rândul lumii T2, adăugat rândul Q31; E1 și E17 completate, intro-ul L6, E5, E15, E16 și sinteza din §5 aduse la lumea T2 livrată, E30 și E31 noi |

## 0. Principiul

Cele patru limite din §7 nu pot fi **eliminate**: sunt proprietăți ale lumii fizice (dispozitivul e al utilizatorului), ale rețelei (cineva poate vedea tot) sau ale încrederii (un membru care a primit cheia a primit și conținutul). Ce se poate face pentru fiecare, pe patru straturi:

1. **Prevenire** — scade probabilitatea evenimentului.
2. **Limitarea pagubei** — când se întâmplă, expune cât mai puțin (blast radius).
3. **Detectare** — utilizatorul și grupul află devreme.
4. **Recuperare** — există o cale înapoi la o stare sigură, fără a pierde identitatea.

Plus un strat **non-tehnic**: norme, stimulente economice, disclosure onest. Regula CP-10 rămâne: nimic din ce urmează nu se afirmă în UI până nu are test sau review.

---

## L1. Dispozitiv compromis

**Ce poate face atacatorul:** citește tot ce vede utilizatorul (ecran, tastatură, notificări), extrage baza de date locală dacă are root, extrage cheile dacă nu sunt în hardware, se dă drept utilizator.

**De ce e ireductibil:** aplicația rulează pe sistemul atacatorului. Orice verificare „anti-root” rulează și ea pe acel sistem și poate fi mințită.

| Strat | Mitigare | Prio | Evidență |
|---|---|---|---|
| Prevenire | Cheile de identitate și cheia DB în **Android Keystore hardware-backed (StrongBox când există)**, neexportabile, cu autentificare biometrică/PIN per deblocare. Un atacator cu root citește DB-ul deschis, dar **nu poate exporta cheile** ca să impersoneze utilizatorul de pe alt dispozitiv | P0 (FR-1.7) | test: cheia are `isInsideSecureHardware`; export eșuează |
| Prevenire | **Auto-lock** agresiv (implicit 1 min fundal) + PIN de aplicație; FLAG_SECURE permanent; mesaje fără selecție sau copiere; câmpuri sensibile marcate pentru accesibilitate; clipboard sensibil cu ștergere automată; IME incognito; avertisment tastatură terță; fără link previews | P0 (ADR-08, ADR-27) | T10, T24, UI tests |
| Prevenire | Recomandare în onboarding: GrapheneOS / dispozitiv dedicat pentru profiluri de risc ridicat; explicat de ce | P0, non-tehnic | review UX |
| Limitare | **Mesaje efemere implicit** (7 zile în DM, configurabil per canal): un dispozitiv compromis azi expune doar ultimele 7 zile, nu istoricul | P0 (FR-4.4 devine default-on; ADR-27) | T25 |
| Limitare | **Pseudonime per canal** (ADR-04): compromiterea nu leagă automat toate canalele de o persoană, dacă nu a făcut „reveal” | P0 | T14 |
| Limitare | **Post-compromise security** din libsignal și MLS: după ce atacatorul pierde accesul, ratchet-ul se vindecă singur; documentăm că PCS nu ajută cât timp accesul persistă | P0 (biblioteci) | vectori bibliotecă |
| Detectare | **Atestare hardware a cheii de identitate** (Android Key Attestation): lanțul de atestare se publică în prekey bundle; contactul vede în safety view „cheie în hardware: da/nu”. Nu detectează malware, dar exclude clasa „cheie copiată pe alt telefon” | P1 | ADR-14 |
| Detectare | **Lista de dispozitive și sesiuni** vizibilă utilizatorului; orice dispozitiv nou al unui contact = avertisment blocant (FR-4.5); safety number verificabil offline | P0 | teste schimbare cheie |
| Recuperare | **Certificat de revocare** semnat cu cheia de identitate (sau derivat din mnemonic pe alt dispozitiv), publicat ca blob în namespace-ul identității: contactele și grupurile îl preiau la sync și scot dispozitivul/identitatea; utilizatorul re-derivă identitatea din mnemonic pe un dispozitiv curat și trece prin **re-verificare** a safety number cu contactele | P0 (FR-1.9) | test: dispozitiv revocat nu decriptează epoch-uri noi |
| Recuperare | **Duress PIN**: un PIN secundar care șterge cheile locale și afișează un profil gol; explicat onest că ștergerea pe flash e best-effort (§8.1) | P2 | test wipe |
| Non-tehnic | Disclosure în UI, în limbaj simplu: „Dacă telefonul tău e compromis, GHOST nu te poate proteja. Iată ce reduce paguba: …” | P0 (CP-10) | review legal/UX |

### L1.1 Reziduuri locale ale sincronizării și ale entitlement-ului (Faza 7, ADR-20 punctul 6; Faza 8, ADR-23 … ADR-24)

Tot ce urmează e în SQLCipher, deci vizibil unui atacator cu baza deschisă (AD-8), în afară de ultimele două rânduri. Rândurile marcate „Faza 8” sunt ale modulului `:entitlement` (schema v3); sămânța, cheia de revendicare, r, mesajele blindate și semnăturile blind nu ies niciodată din Rust, iar sămânța și cheia de revendicare se șterg la finalizare. Nu se persistă programe, chei PRF, breakere, latențe, istoric de cereri sau stare de sănătate (T20). Starea și cache-ul Arti (`noBackupFilesDir/tor`, Faza 6) nu sunt stare de sync și nu apar aici.

| Reziduu | Ce vede un atacator cu baza deschisă | Cât timp | Evidență |
|---|---|---|---|
| Tombstone-uri de deduplicare (numai namespace-uri ascultate) | (namespace, hash, zi de ștergere) pentru blob-urile primite și proprii; fără conținut, fără ordine de sosire (tabelă WITHOUT ROWID), fără distincția proprii/primite; nimic pentru namespace-urile doar de scriere | primite: până la `ceil7(zi(expirarea la relay) + 24)`, adică 24–30 de zile după expirarea blob-ului la relay; listate dar neaduse: `ceil7(ultima zi de listare + 111)`; proprii: cât există operația (ridicat la fiecare confirmare la `ceil7(zi(expirare) + 24)`), apoi `ceil7(azi + TTL + 8)`. Rândul e șters la prima trecere GC după acea zi; GC-ul rulează numai cât sesiunea e online și ceasul de perete a mers împreună cu ceasul monoton de la ultimul READY (design §11.5 punctul 2), deci un dispozitiv offline păstrează rândurile mai mult. Termenii derivă din fereastra de stocare H = 7 zile și din toleranța de ceas de 3 zile; se scurtează doar scurtând H (Q4) | `store/SchemaIntrospectionTest`, `store/GcRetentionTest`; verificarea de siguranță a retenției din harness (mutantul M8 e detectat) |
| Ciphertext E2E neconsumat | blob-urile aduse și încă neconsumate, criptate E2E | până le marchează consumatorul; nu se șterg automat (fără pierderi); `SyncStatus.expiredUnconsumed` le numără pe cele trecute de ziua de retenție. În Faza 7 nu există încă consumatori | `store/GcRetentionTest` |
| Outbox în curs | operațiile nedecise, cu ciphertext-ul înghețat (necesar ca reîncercările să trimită aceiași bytes), și rezultatele neeliberate | ciphertext-ul: până la ultima trimitere posibilă (trigger); rândurile: până la eliberare și ștergerea de către GC | triggerele schemei v2 (`outbox_op_payload`, `outbox_op_delete`), `store/GcRetentionTest` |
| Starea de reîncercare a lucrului în curs | `attempts`, `next_attempt_minute`, `lease_hour`, `copy_hour`, `ack_minute`, `strikes` (livrări outbox); `fetch_attempts`, `next_fetch_minute`, `offers`, `offer_after_minute` (inbox): numere de încercări și momente la minut sau oră | inbox: până blob-ul e adus sau consumat (contoarele revin la 0); outbox: până la ștergerea operației | `store/SchemaIntrospectionTest` (granularitate impusă de CHECK) |
| Cursoare, capabilități, director de relay-uri | cursorul opac per (relay, namespace), token-urile de capabilitate, adresele onion și id-urile de operator ale relay-urilor cunoscute, seturile de relay-uri ale namespace-urilor | cât sunt folosite; capabilitățile: până la expirare + 1 zi; relay-urile retrase: 111 zile după retragere, dacă nimic nu le mai referă | `store/GcRetentionTest` |
| Token-uri nefolosite (Faza 8, `ent_token`) | token-uri de acces, de invitație și de credit: valoare la purtător, nelegabile de plată prin octeți (sunt blind); `eligible_minute` al unui token proaspăt arată ziua în care s-a finalizat lotul lui (E19); cât o răscumpărare e în curs, relay-ul, namespace-ul și `request_id` rezervate | cât sunt folosibile: un token de acces până la sfârșitul ferestrei săptămânii lui (cel mult ~5 săptămâni după finalizare), un credit până la 65 de săptămâni; un token cheltuit, pierdut sau ieșit din fereastră se șterge (nu e marcat „cheltuit”); tabela e cheiată pe nullifier, fără ordine de inserare și fără legătură cu cumpărarea | `EntitlementSchemaIntrospectionTest`, `EntitlementSchemaConstraintsTest`, triggerele v3; GC (`ClaimAndGcTest`) |
| Cumpărare deschisă (Faza 8, `ent_purchase`) | factura, subadresa, suma, sămânța, cheia de revendicare, ora creării, minutul primirii facturii, suma rămasă: leagă dispozitivul de o plată **cât timp cumpărarea e deschisă** | până la starea terminală: tranzacția terminală șterge secretele, factura, subadresa, suma, ora creării, minutul primirii și suma rămasă (CHECK); rândul terminal (tip, stare, ziua terminală) se șterge la ziua terminală + 7 | CHECK-urile v3; harness-ul `:entitlement` (R10: nicio coloană secretă nenulă într-o stare terminală) |
| Invitații create și ținta drop-ului (Faza 8, `ent_invite`, `ent_drop_target`) | indicii invitațiilor create, namespace-ul drop-ului fiecăreia și ziua până la care se ascultă (payload-ul doar cât invitația e `created`); la invitat: namespace-ul, cheia și sloturile drop-ului celui care invită și momentul tras `t_drop` | invitațiile: până la `listen_until_day` (expirare + 56 de zile), apoi GC; ținta drop-ului: până la scrierea blob-ului și eliberarea rezultatului | triggerele v3, `DropStepsTest` |
| Revendicări și adrese de plată (Faza 8, `ent_claim`, `ent_payout_used`) | adresa de plată cât revendicarea e `prepared`; hash-uri sărate (HMAC cu sarea locală `payout_salt`) ale adreselor folosite deja | adresa: până la răspunsul issuer-ului; hash-urile: 365 de zile (Q10) | `ClaimAndGcTest`, triggerul `ent_claim_guard` |
| Momentul ecranului de plată (Faza 8, `ent_state.payment_shown_minute`) | minutul (rotunjit în sus) în care ecranul de plată a fost vizibil ultima dată, ca un proces nou să refacă reținerea sesiunilor cu relay-uri (ADR-23) | cel mult 60 de minute, apoi GC îl golește | `ClaimAndGcTest` |
| Memoria ES (Faza 8, `ent_key`, `ent_schedule_fact`) | cheile, mulțimile de sloturi, prețurile și revocările acceptate: date publice ale ES-ului, nimic despre utilizator | pentru totdeauna (append-only, regula 5 a ES-ului) | triggerele append-only, `ScheduleAcceptanceTest` |
| Intrarea jobului în JobScheduler (`/data/system/job/jobs.xml`, în afara SQLCipher, accesibilă cu root) | un singur job periodic (componentă, perioadă, constrângeri, momente de rulare), fără extras | până la wipe (`onWipe` anulează jobul) | `android/PeriodicJobSpecTest`; inventarul pe dispozitiv: manual până la CI pe emulator (Faza 13) |
| Statisticile OS de rețea și baterie (`/data/system/netstats`, batterystats; scrise de sistem, nu de stratul de sync, în afara SQLCipher, accesibile cu root) | tot traficul relay-urilor e atribuit UID-ului aplicației, iar sistemul păstrează volumul rx/tx per UID pe intervale de timp (de ordinul a 2 ore). Volumul pe interval urmează trimiterile și traficul primit, **în ambele moduri**: high-privacy mode întârzie trimiterile, dar nu umple volumul (L2.1 punctul 3), până la cover traffic (P2). batterystats păstrează durata fiecărui job și timpul de rețea și de wakelock per UID; o sesiune de fundal rulează până își golește banda de lucru, deci durata jobului crește cu munca golită. Din ele se poate reconstrui o cronologie a activității (când s-a trimis sau primit mult), fără conținut, namespace-uri sau destinatari | cât le păstrează sistemul (săptămâni) | în afara ariei T20, care acoperă doar ce scrie stratul de sync în afara SQLCipher (numai intrarea jobului); se reduce doar cu cover traffic și joburi de durată fixă (Faza 12, P2); design §6.3 și §11.5 punctul 6 |

---

## L2. Adversar global pasiv (GPA)

**Ce poate face:** vede toate legăturile din rețea; corelează momentul și volumul intrării tale în Tor cu momentul și volumul ieșirii către un relay `.onion`. Tor nu apără împotriva acestui adversar prin design.

**De ce e ireductibil:** orice sistem cu latență mică și trafic dependent de activitatea utilizatorului scurge un semnal statistic. Singura apărare completă e traficul constant, independent de activitate, cu costuri de baterie/rețea.

| Strat | Mitigare | Prio | Evidență |
|---|---|---|---|
| Prevenire | **Padding pe bucket-uri** 1/4/16/64 KiB (ADR-09) | P0 | T9 |
| Prevenire | **Polling la ritm constant, independent de activitate**: clientul cere loturi de dimensiune fixă la intervale fixe + jitter, indiferent dacă utilizatorul a scris sau nu. Un observator vede același tipar de la un utilizator inactiv și de la unul activ. Costul: baterie (de măsurat, NFR-5) | P1 | ADR-15; Faza 7: T19 (JVM) arată că momentele listărilor nu depind de activitate; volumul încă depinde (L2.1). T17 (distribuția idle vs activ statistic indistinctă): Faza 12, doar în high-privacy mode cu cover traffic (P2), ADR-20 |
| Prevenire | **Trimitere întârziată („mixing” local)**: forumul nu e real-time; postările pot pleca cu întârziere aleatoare 0–N minute (setare per canal, implicit pornită în high-privacy mode), ceea ce rupe corelarea „a apăsat send la 12:03:41 → blob apărut la relay la 12:03:43” | P1 | ADR-15; aplicată în Faza 7: U[0, 10 min] eșantionată o dată per operație, la minut, implicit numai în high-privacy mode, cu suprascriere per namespace (`SendDelay`; `engine/SendDelayTest`); distribuția se măsoară în Faza 12 |
| Prevenire | **Circuite izolate** per canal/scop și **guard diversity** (Arti) | P0 (ADR-01) | test izolare |
| Prevenire | **Bucketizare la citire**: mai multe canale împart același bucket de fetch, clientul filtrează local (PIR-lite) — observatorul relay-ului vede bucket-ul, nu canalul | P2 | ADR-15 |
| Prevenire | **Cover traffic** cu politică conștientă de baterie (FR-2.6) | P2 | măsurare NFR-5 |
| Limitare | Chiar dacă GPA leagă „acest IP folosește GHOST”, nu află **cu cine** vorbește sau **ce**: conținut E2E, relay-uri blind, entitlement nelegabil. Paguba maximă = „este utilizator GHOST” + tipar de activitate | — | matricea de garanții |
| Non-tehnic | Disclosure: „rezistență, nu imunitate” (CP-07). Pentru utilizatorii cu adversar statal se recomandă explicit: rețea publică, nu cea de acasă; bridges; dispozitiv dedicat | P0 | review UX |

### L2.1 Reziduuri ale sincronizării (Faza 7, ADR-20; design §6.3)

1. **Co-apariția la începutul sesiunii.** Toate perechile (relay, namespace) ale unui client pornesc în primele 30 s după deschiderea aplicației și în primele 90 s ale fiecărui job de fundal. Un relay care găzduiește mai multe namespace-uri ale aceluiași client le vede începând împreună. Remediere: bucketizarea la citire (ADR-15, P2).
2. **Legabilitate per namespace prin capabilitate și cursor.** Toate cererile unui client pentru un namespace sunt legabile prin `capability_scope` (observabil permis) și prin cursoarele emise de relay; un relay rău-intenționat poate folosi cursoare unice drept cookie între circuite și sesiuni. Remediere: capabilități de citire partajate (decizie în Faza 8) și ancore de cursor partajate (Faza 12). Înregistrat în THREAT_MODEL §6, rândul „Citire canal”. **Decizie în Faza 8 (ADR-25, D11):** capabilități de citire partajate per (namespace, epocă), înregistrate la relay prin hash (`RegisterReadKey`), implementate în Fazele 9 și 10 odată cu primii consumatori; până atunci citirea folosește capabilitatea de scriere; token-urile nu cumpără capabilități de citire. Decizia e luată, efectul nu: până în Faza 9 cererile de citire rămân legabile per namespace ca mai sus, iar cursoarele-cookie rămân pentru Faza 12.
3. **Volumul urmează activitatea** până la cover traffic (P2): în modul standard trimiterile pleacă imediat ce sunt scadente, iar get-urile urmează scrierile altora în ambele moduri. Doar momentele listărilor sunt independente de activitate (T19).
4. **Prezența.** Cadența din prim-plan (un eveniment la ~30 s per pereche) diferă de cea din fundal (joburi OS, ~15 min sau mai rar), deci un observator distinge aplicația deschisă de cea închisă.
5. **NFR-1.** Ținta NFR-1 (DM p95 < 2 s, p99 < 5 s) nu e atinsă: cu interval de 30 s, latența în prim-plan e estimată în design la ~40 s p95 (nemăsurată), iar în fundal la ore. ADR-20 punctul 5; decizie Q2, măsurare în Faza 12.
6. **Limită de capacitate.** Peste `BACKLOG_CAP` = 4 096 de rânduri listate dar neaduse per (relay, namespace), cererea de listare pleacă în continuare (programul nu se schimbă), dar pagina e aruncată și cursorul rămâne pe loc. `FETCHED_CAP` = 256 de blob-uri aduse și neconsumate per namespace oprește aducerile noi. Bugetul de aduceri e 8 per eveniment în prim-plan și 32 în fundal; numai în fundal, o pereche nu pierde nimic cât rata ei de intrare rămâne sub 32 × (joburi pe zi) × (zile de TTL), adică circa 21 500 de blob-uri per namespace DM de 7 zile la ~96 de joburi pe zi (Doze reduce numărul). Precondiție: consumatorii golesc coada și dispozitivul e online cel puțin o dată per TTL. ADR-20 punctul 9.

---

## L3. Membru de grup rău-intenționat

**Ce poate face:** a primit cheia canalului, deci **a primit conținutul**: îl poate copia, fotografia ecranul, redistribui; poate face spam/phishing; poate colecta pseudonimele; poate încerca să scoată membri la „reveal”; poate corela momentele de activitate.

**De ce e ireductibil:** nu există criptografie care să dea cuiva acces la conținut și să-l împiedice să-l rețină. „Invalidarea APK-ului” nu e posibilă: nu putem opri software pe telefonul altcuiva, iar un atacator folosește oricum un client modificat.

### 3.1 Ideea propusă: flag-uri de la utilizatori cu prag configurabil

Ideea e bună și **implementabilă** în forma corectă: pragul nu „invalidează APK-ul”, ci declanșează **o operațiune criptografică pe care grupul o poate face**: `Remove` în MLS (epoch nou → membrul nu mai decriptează nimic de acum înainte) plus efecte economice asupra celui care l-a adus. Analiza:

**Ce funcționează**
- Fiecare flag e un **mesaj MLS de aplicație semnat** de pseudonimul flagger-ului: `Flag{target_pseudonym, reason_code, epoch}`. Toate clienții din canal îl văd și îl numără identic (aceeași sursă de adevăr, fără server).
- **Pragul** e în **politica canalului** (obiect MLS semnat de creator/delegați): număr absolut sau fracțiune din membri activi, cu minim absolut (ex. `max(3, 20%)`).
- Când pragul e atins, **orice client cu rol de moderator (sau, în canalele fără moderatori, primul client care observă pragul) emite `Remove`**. Rezultatul: membrul iese, epoch avansează, cheile vechi nu-i mai folosesc pentru mesaje noi. Este exact FR-3.8.
- Cel scos își păstrează entitlement-ul (token-urile sunt nelegabile — nu putem și nu vrem să-l identificăm la issuer). Dar e **afară din acel canal** și, prin propagarea deciziei, poate fi ținut afară din canalele care aleg să accepte lista.

**Ce nu funcționează sau e periculos**
- **Brigading / Sybil**: un atacator cu 3 conturi scoate pe oricine. Într-un sistem plătit și invite-only, fiecare cont costă un abonament și o invitație, ceea ce ridică prețul, dar nu îl face imposibil.
- **Cenzura prin flag**: majoritatea poate scoate o minoritate incomodă. E o decizie de produs: forumul e privat, creatorul canalului răspunde de politica lui; noi dăm instrumentul și îl facem transparent.
- **Flag-urile scurg metadate în grup**: cine pe cine a raportat. Se vede doar în canal (pseudonime), niciodată la relay. Opțional: flag-uri **ascunse** până la prag (commitment publicat imediat, dezvăluire la prag) — mai complex, P2.

**Cum se face rezistent la abuz (propunere ADR-13)**
1. **Sponsor și „strike”**: în MLS, cine adaugă un membru e vizibil (`Add` semnat). Sponsorul răspunde pentru cine aduce: la fiecare membru sponsorizat scos prin prag, sponsorul primește un strike; la K strike-uri pierde dreptul de a adăuga în acel canal; politica poate escalada până la scoaterea sponsorului. Stimulent puternic pentru invitații atente, coerent cu modelul de referral.
2. **Ponderare după vechime**: flag-urile membrilor cu < X zile în canal contează 0 sau 0,5. Un atacator trebuie să aștepte, ceea ce costă abonamente.
3. **Diversitate a flagger-ilor**: pragul cere flag-uri din cel puțin k **subarbori de sponsorizare distincți** (membrii adăugați de același sponsor numără ca unul). Blochează atacul „îmi aduc trei conturi”.
4. **Cool-down și drept la răspuns**: după N flag-uri (sub prag) membrul intră în **carantină**: read-only, mesajele sale ascunse local pentru cine vrea; are un interval să răspundă înainte de `Remove`. Reduce eroarea și cenzura la cald.
5. **Probațiune la intrare**: membru nou = read-only și limită de rată în primele X ore/zile, fără istoric (history policy „none”). Un atacator care intră doar ca să extragă istoricul nu primește nimic.
6. **Listă de excludere semnată, portabilă, opt-in** (§11.2 din spec): canalul publică lista pseudonimelor scoase; alte canale ale aceluiași „creator/comunitate” pot importa lista. Deoarece pseudonimele sunt per canal (ADR-04), portabilitatea reală cere o **verificare de identitate la intrare**: canalele „cu verificare” cer la `Add` un commitment la identitatea de bază (hash + dovadă), astfel încât o excludere se aplică persoanei, nu pseudonimului. Este un compromis explicit între unlinkability și reputație, **ales per canal și afișat membrilor** înainte de a intra.

### 3.2 Mitigări independente de flag-uri

| Strat | Mitigare | Prio |
|---|---|---|
| Prevenire | Canale mici, invite-only; **politica de istoric „fără istoric pentru membri noi”** implicită | P0 (FR-3.8) |
| Prevenire | **Probațiune** (read-only + rate limit) pentru membri noi | P1 |
| Prevenire | „Reveal” (legarea pseudonimului de identitatea de bază) e **opt-in, per persoană, cu avertisment**: un membru rău nu poate forța pe nimeni la deanonimizare | P0 (ADR-04) |
| Limitare | **Mesaje efemere per canal** (P0, Faza 10) + fără screenshot, selecție sau copiere în aplicație (P0, Faza 13) + media „vezi o singură dată” (P1, Faza 11); fotografia ecranului cu alt dispozitiv și un client modificat rămân posibile (ADR-27) | P0 / P1 |
| Limitare | Pseudonime per canal: leak-ul dintr-un canal nu deanonimizează participarea în altele | P0 |
| Detectare | **Traitor tracing pentru media** (P2, cercetare): pentru fișiere marcate „sensibile”, expeditorul criptează **o variantă cu filigran invizibil per destinatar** (cost N× pentru grupuri mici); un leak identifică pseudonimul sursă. Pentru text nu există echivalent robust la screenshot; se documentează ca limită | P2 |
| Detectare | Flag-uri + carantină (3.1) | P1 |
| Recuperare | `Remove` MLS, strike pe sponsor, listă de excludere; **rotire** a cheii canalului la fiecare scoatere (inerent MLS) | P0/P1 |
| Non-tehnic | Reguli de canal afișate la intrare; rolul moderatorilor; termeni: GHOST nu poate șterge ce a fost deja copiat | P0 |

---

## L4. ISP-ul vede că folosești Tor

**Ce vede:** conexiuni către relay-uri Tor cunoscute (listă publică) sau tipare de trafic Tor. Nu vede destinația, conținutul sau GHOST ca aplicație.

**De ce e ireductibil:** cineva trebuie să transporte pachetele. Se poate ascunde *ce* protocol e, nu *că* există trafic.

| Strat | Mitigare | Prio | Evidență |
|---|---|---|---|
| Prevenire | **Bridges + pluggable transports**: WebTunnel (trafic care arată ca HTTPS către un site obișnuit), obfs4, Snowflake (WebRTC). Arti le suportă; livrăm un set de bridges implicite în manifestul semnat și rotim | P1 (ADR-16) | test: captură nu conține fingerprint Tor |
| Prevenire | **Mod „auto”**: clientul încearcă Tor direct; dacă e blocat sau utilizatorul alege „ascunde utilizarea Tor”, trece pe bridges; niciodată pe clearnet | P1 | test fail-closed |
| Prevenire | Opțiunea **Tor peste VPN** (VPN-ul utilizatorului): ISP-ul vede VPN, furnizorul VPN vede Tor; explicat ca mutare a încrederii, nu eliminare | P1, non-tehnic | disclosure |
| Prevenire | **Polling la ritm constant** (L2): utilizarea GHOST nu se distinge de orice altă utilizare Tor prin tipar temporal. Obiectiv încă neatins: bootstrap-urile Tor periodice din Faza 7 sunt o amprentă temporală (rândul „Conflict” de mai jos) | P1 | ADR-15 |
| Conflict (Faza 7) | **Bootstrap-uri Tor periodice.** Fiecare job de fundal (~15 min, moment ales de OS) creează un transport nou și face bootstrap Tor, și în idle. Tiparul „un bootstrap Tor la fiecare job” e o amprentă temporală a aplicației pentru ISP, chiar dacă destinația rămâne ascunsă | — | ADR-20 punctul 10; mitigări în Faza 12: offset aleator al perioadei per instalare, păstrarea transportului între joburi |
| Limitare | Chiar dacă ISP-ul știe „Tor”, nu știe „GHOST”: aplicația nu are domenii, IP-uri sau certificate proprii pe clearnet; distribuția prin F-Droid repo/onion mirror | P0 (ADR-07) | T6 |
| Non-tehnic | Ghid regional în app: unde utilizarea Tor e riscantă legal, recomandăm bridges implicit și disclosure clar; nu promitem invizibilitate | P0 | review legal |

---

## L5. Riscuri reziduale ale implementării (adăugat după review-ul Fazei 6, 2026-09-11)

| Risc | Stare | Plan |
|---|---|---|
| **PoW pe onion service nerezolvat de clienți.** Relay-ul poate cere proof-of-work la introducere (Prop 327); clientul Arti nu compilează `hs-pow-full` (experimental). Sub un flood de introduceri, clienții GHOST nu primesc prioritate și pot fi refuzați ca oricine altcineva (DoS, AS-11) | acceptat temporar (ADR-01, torrc) | reevaluare când `hs-pow-full` devine stabil în Arti; gate-ul de feature-uri împiedică activarea accidentală |
| **Bridges doar simple până în Faza 12.** O linie `IP:PORT FINGERPRINT` ascunde relay-ul de gardă, nu faptul că traficul e Tor (AD-4) | cunoscut | transporturi pluggable (lyrebird/obfs4, snowflake, webtunnel) în Faza 12, ADR-16 |
| **Keystore Arti compilat și deschis** (gol) | test unitar (după creare) și test live (după bootstrap și conexiuni onion, jobul CI `live-tor`) | vezi `deny.toml` (RUSTSEC-2023-0071) |
| **Build nativ reproductibil doar la aceeași cale** | parțial | builder cu căi fixe în Faza 14 (ADR-07) |
| **Sync: încrederea în ceas după READY** (Faza 7). Până la review-ul S9, după primul READY ceasul era considerat de încredere pentru tot procesul (`readyInProcess`), deci un salt mare înainte, cu transportul READY sau după o cădere a lui, lăsa închiderea ferestrelor (M3), deciziile D2 și GC-ul să ruleze pe ceasul sărit | **corectat** după review-ul S9 (design §11.5 punctul 2): M3, D2 și GC rulează numai cât sesiunea e online și ceasul de perete a mers împreună cu ceasul monoton de la ultimul READY (abatere de cel mult 1 h); peste, ceasul nu mai e de încredere până la următorul READY. Teste: `engine/ClockTrustTest`, scenariul S-K din harness. Rămâne premisa „READY ⇒ ceas la ±3 zile”, pe care o dă Arti | niciunul în Faza 7; un salt sub 1 h rămâne în toleranța σ = 3 zile |
| **Sync: o sursă `bad` scoate un blob de pe acel relay** (Faza 7). Un singur `malformed_response` sau două `not_found` de la un relay fac sursa `bad`; fără alt candidat, rândul devine `unavailable`, iar cursorul nu îl mai re-listează de pe acel relay. O eroare tranzitorie a unui relay onest pierde blob-ul de pe acel relay până îl listează alt relay al setului | conform designului (§4.2, §11.3), sub premisa relay-urilor oneste | reevaluare în review-ul S9 |
| **Sync: verificări doar pe dispozitiv** (design §8.9): SQLCipher real (migrarea v1→v2, triggere, pragma-uri), legarea `SyncJobService` neexportat pe API 29 și 37, jobul persistent după reboot și force-stop, inventarul T20 după wipe, Tor real, rularea live cu 3 relay-uri de staging și `am kill` repetat | neexecutate; pe JVM sunt emulate (sqlite-jdbc, model de relay, transport fals) | manual până la CI pe emulator (Faza 13) |
| **Entitlement: tăierea jurnalului issuer-ului** (Faza 8). După valul D, `FileJournal::prune` exista, dar nimic nu îl rula, deci `issued.journal` creștea, iar retenția de 7–14 zile (ADR-26 punctul 5) nu era aplicată | **închis** în valul E (design §19.24 punctul 11, §19.25): `ghost-issuer-ops journal-prune`, rulat orar de `journal-prune.sh` din cron după cel mai nou instantaneu verificat (`RUNBOOK.md` B1); segmentul ultimei intrări rămâne mereu. Rămân: fără un instantaneu verificat nu se taie nimic, iar un issuer fără tranziții păstrează segmentul ultimei intrări (E31, Q32) | de verificat pe un host Docker real: că `DAC_OVERRIDE` ajunge pentru ștergerea fișierelor issuer-ului și că rularea orară nu scrie pe stderr (zgomot în mail-ul cron-ului); Q32 (intrarea-ancoră) în valul următor |
| **Entitlement: numărătorile relay-urilor pentru R2** (Faza 8). După valul D, `nullifiers.redb` ținea rândurile pe săptămână, dar `ghost-relay` nu le exporta, deci verificarea independentă „suma pe sloturi ≤ token-urile de acces semnate” a reconcilierii R2 nu avea date de la relay-uri | **închis** în valul E (design §19.24 punctul 10): tabela `redemption_counts` din `nullifiers.redb` (numărul final al fiecărei săptămâni închise, ultimele 13) și `ghost-relay serve --redemption-counts <fișier>`, exportat săptămânal (`RUNBOOK.md` O1, R2). Rămân: săptămânile închise înainte ca depozitul să aibă tabela și cele de dinaintea unui `--nullifiers-reset` nu au număr | de verificat pe relay-urile de staging (`docker compose exec -T relay-<x> cat …`) |
| **Entitlement: onion-ul issuer-ului la instalare** (Faza 8). După valul D, verificarea căuta `<onion>:443` în octeții ES-ului; arăta că ES-ul listează onion-ul servit de Tor, nu că îl listează ca `issuer_onion` | **închis** în valul E (design §19.24 punctul 12): `ghost-issuer-ops schedule-onions` afișează `ISSUER_ONION onion=… port=…`, iar verificarea de instalare caută exact acea linie (`RUNBOOK.md` §2; un test arată că ea potrivește onion-ul issuer-ului o dată și niciodată pe al unui relay) | — |
| **Entitlement: verificarea ceasului față de consensul Tor** (Faza 8). Verificarea opțională `CLOCK_UNTRUSTED` (design §19.4 punctul 4) nu e implementată: Arti 0.46 expune durata consensului doar sub `experimental-api`. Un dispozitiv decalat cu peste 4 h își dezvăluie decalajul issuer-ului la primul apel care poartă `base_week` (E14) | acceptat (§19.22 punctul 3) | reevaluare când Arti expune durata consensului stabil |
| **Entitlement: sesiunea de fundal fără pereche de citire** (Faza 8). O sesiune de fundal fără nicio pereche de citire se încheia înaintea primului pas al benzii de răscumpărare, deci un client ale cărui capabilități au expirat toate răscumpăra doar în prim-plan | **implementat** (Q29; design §19.23 punctul 5, §19.24 punctul 13): o sesiune de fundal care pornește cu o nevoie de scriere în așteptare rămâne deschisă până la primul pas al benzii (`RedeemHold`); reziduul nou E30. Rămâne: nevoile doar de citire nu armează reținerea, deci un client care doar ascultă și ale cărui capabilități au expirat toate își revine doar în prim-plan sau într-o sesiune mai lungă | Q34 (propus: doar nevoile de scriere în Faza 8, reevaluare cu cheile de citire partajate din Faza 9, D11) |
| **Entitlement: trialul în high-privacy mode** (Faza 8). Zilele suplimentare Geometric(1/2) ale slotului de activare puteau împinge eligibilitatea token-urilor unui trial dincolo de ambele lui săptămâni, deci un invitat în high-privacy mode își putea pierde trialul | **implementat** pentru trialurile de onboarding (Q30; design §19.23 punctul 5, §19.24 punctul 13; `Slots.trialEligibleMinute`); efectul asupra E1 e declarat. Rămân: un trial finalizat atât de târziu încât slotul lui cade după ultima zi a trialului rămâne nefolosibil; rezervele unei revocări (design §8.6) urmează regula pachetului fără plafon, deci în high-privacy mode pot deveni eligibile după săptămânile lor și se pierd | acceptat (se pierd doar rezerve ale celui care invită, nu trialul invitatului) |
| **Entitlement: scanarea drop-urilor după restaurare** (Faza 8). Design §8.4: după o restaurare, clientul ascultă drop-urile invitațiilor 0..7 timp de 5 săptămâni (`ent_state.restore_scan_until_day`). Coloana există în schema v3, dar nimic din `:entitlement`, `:identity` sau `:sync` nu o scrie și nu o citește, deci un client restaurat nu ascultă niciun drop al invitațiilor create înainte, iar creditele trimise în ele se pierd (E11) | deschis | scanarea în `:entitlement`: coloana scrisă la restaurare, drop-urile 0..7 înregistrate ca ascultate până la acea zi; după implementare, rândul se închide și E11 revine la formularea designului |
| **Entitlement: poarta T2 la scara gate** (Faza 8, slice-ul S10). Lumea T2 pe sistemul complet e livrată (design §19.24 punctul 9; INVARIANTS T2): J1–J10, T2b, T2c, NI-1, NI-1d, NI-2, NI-3, S1–S4 (inclusiv S3d), cei 23 de mutanți ai lumii și porțile ei CI. La integrarea valului E, varianta PR (N = 300) și mutanții trec în rulări locale release, dar nicio rulare la scara gate (N = 2 000) nu a trecut încă pe codul revizuit. Lumea modelează acum reținerea Q29 (E30 e măsurat și raportat, linia E30 a raportului) și trage slotul rezervelor unei revocări cu regula pachetului, ca clientul (design §19.26 punctele 2 și 3). Bugetele de timp depășesc estimarea designului și nu sunt măsurate pe runner-ul CI (Q35); citirea J8 per relay așteaptă confirmarea (Q33). `rust-gates.sh` nu rulează T2 (lumea e ignorată în profilul debug): o lume stricată o prind doar jobul CI `rust` și `t2-exit-gate` | parțial | o rulare verde înregistrată a `t2-exit-gate` (condiție de închidere, design §15.2); până atunci nimic din L6 nu se afirmă în UI (CP-10) |
| **Entitlement: momentul reîmprospătării unui credit primit** (Faza 8, Q31). Reîmprospătarea era scadentă la 1–14 zile după ce clientul celui care invită a citit creditul din drop, o citire pe care relay-urile o văd: un apel către issuer temporizat de activitatea la relay-uri, în nicio celulă L | **implementat** (Q31 decisă; design §19.26 punctul 1): la crearea invitației clientul trage două momente (`ent_invite.refresh_minute`, `late_refresh_minute`): primul la 1–14 zile după sfârșitul ultimei ferestre de drop a unui invitat al invitației, al doilea la 1–14 zile după sfârșitul ascultării; un credit citit până la primul așteaptă primul, unul citit după el primește al doilea; tăierea de la §19.8 rămâne, iar un credit a cărui scadență ar precede citirea se aruncă. Gemenii NI-2 și NI-3 citesc la propriile momente, iar raportul T2 nu mai numește nicio excepție | citirea mai decide un singur bit, declarat în E17 (L6) |
| **Entitlement: verificări doar pe dispozitiv** (Faza 8, design §11.9): SQLCipher v2 → v3 pe o bază reală, rulările liniștite sub Doze, JNI pe ambele ABI, Tor real către issuer-ul și relay-urile de staging, scanarea codului QR de 876 de caractere; un apel real la onion-ul issuer-ului de staging și o răscumpărare prin Tor | neexecutate; pe JVM sunt emulate (`TestTokenCrypto`, `ModelIssuer`, `ModelRedeemRelay`); `live-tor` dovedește doar circuitele distincte ale fluxurilor `IssuerFlow` | manual până la CI pe emulator (Faza 13); rularea de staging după prima pornire a instalării (`RUNBOOK.md` §13) |

## L6. Entitlement (Faza 8, ADR-22 … ADR-26)

**Ce poate face atacatorul:** sub AD-1, operatorul are issuer-ul, portofelul lui de vizualizare și, în cel mai rău caz, toate relay-urile; marchează totul la precizie completă și încearcă să lege o plată de clusterul de activitate al plătitorului la relay-uri. AD-8 și AD-12 văd starea locală (L1.1).

**De ce nu dispare complet:** semnătura blind scoate octeții token-ului din orice join, iar dovada de permutare și ES-ul fixat scot cheile, prețurile și perioadele alese per client (ADR-22). Rămân momentele: când plătești, când folosești prima dată un lot, când vorbește clientul cu issuer-ul. Designul le reduce la scurgerea declarată L1–L7 (INVARIANTS, nota T2); T2 verifică exact că nimic altceva nu trece (J1–J10, NI-1, NI-1d, NI-2, NI-3, NI-K) și statistic cât rămâne (S1–S3 la α = 0,001; S4 ≤ 0,25 pentru ce permite L însăși), în lumea T2 pe sistemul complet (slice-ul S10, livrat; INVARIANTS T2) și pe motorul Kotlin real (NI-K). Varianta PR rulează la fiecare PR, dar o rulare verde înregistrată la scara gate lipsește încă (L5); reîmprospătarea unui credit primit nu mai urmează citirea din drop (Q31, design §19.26), care alege doar unul dintre două momente trase dinainte (E17). Nimic din această secțiune nu se afirmă în UI până la o rulare verde înregistrată a porții T2 (CP-10); textul pentru utilizator vine în Faza 13.

| ID | Reziduu | De ce rămâne | Atenuare și starea după Faza 8 |
|---|---|---|---|
| E1 | Ziua slotului de activare al primei folosiri a unui pachet (L1) | slotul estompează, nu elimină, legătura cumpărare → prima folosire pentru reluări și genesis | slot = prima graniță de zi UTC ≥ finalizare + 4 h, plus U[0, 6 h); în high-privacy mode plus zile Geometric(1/2); reînnoirile cumpărate din timp nu dau semnal; invitații: onboarding finanțat de trial și scrierea în drop la un moment tras la activare, independent de cumpărări. În high-privacy mode zilele suplimentare ale unui trial de onboarding se opresc la ultima zi a săptămânii base + 1 (Q30): un trial finalizat târziu în base + 1 are mai puțină estompare, deloc dacă e finalizat în (vineri 20:00, sâmbătă 20:00] UTC, când prima folosire e mereu duminică 00:00 + U[0, 6 h) a lui base + 1; un issuer care eșuează `RedeemInvite` până la un prim-plan târziu (în cele 40 de încercări) poate face plafonul să se aplice (E8 × E1); cu plafonul c, șansa ca prima folosire să cadă în ultima zi cel mult se dublează (2^−(c+1) → 2^−c), iar fără plafon aceeași masă lăsa trialul nefolosibil (design §19.23 punctul 5) |
| E2 | Săptămâna de sfârșit a acoperirii (L2) | partiție prin design | un singur produs; acoperirea se termină la o graniță de săptămână comună |
| E3 | Apelurile imediate cerute de utilizator și începutul trialului (L3) | utilizatorul a cerut viteză; onboarding-ul cere un apel | butoanele „factură acum” și „verifică acum” sunt opționale, doar în modul standard, cu aplicația vizibilă; trialul de onboarding se reîncearcă la fiecare prim-plan, cel mult de 40 de ori (fiecare încercare e un eșantion L3) |
| E4 | Tipul cumpărării: XMR, credite, trial (L4) | issuer-ul trebuie să știe cum s-a plătit | — |
| E5 | Golurile rulărilor liniștite, cumulate per factură (L5) | un apel către issuer trebuie să aibă loc cândva; apelurile unei facturi sunt legate prin `invoice_id` | q = 1/8, extrageri independente de cumpărări; ≤ 3 biți per apel (mult mai puțin sub Doze); de obicei 2 apeluri per cumpărare, niciodată peste 6 (`RequestInvoice` cu cel mult o reîncercare, `BlindSign` cel mult 5; o factură primită la reîncercare își începe planul `BlindSign` de la a doua fereastră, deci cu cel mult 4 încercări), orice ar răspunde issuer-ul, deci ≤ 18 biți în cel mai rău caz față de un issuer care minte; biții măsurați per factură intră în raportul T2 (S3c, raportat), iar S4 îi mărginește (lumea T2; rularea verde la scara gate încă neînregistrată, L5) |
| E6 | Onboarding-ul genesis e legabil la volumul alpha | seturi mici (tabelul L6.1: 2 onboarding-uri pe zi cu sloturi zilnice → set așteptat 3, singur 13,5 %) | genesis e doar pentru bootstrap, cu puțini utilizatori; invitații trec prin trial |
| E7 | Plata de la un exchange cu KYC leagă o persoană de o factură | în afara GHOST | avertismentul FR-6.8 înaintea URI-ului; E1–E5 rămân valabile separat |
| E8 | Un issuer activ poate refuza sau întârzia serviciul pentru a izola un client (n−1) | controlează serviciul | rulări liniștite; cel mult 5 încercări `BlindSign` per factură, la momente trase dinainte din sămânță, și cel mult 2 pentru celelalte apeluri automate, deci eșantioanele pe care le poate forța sunt mărginite |
| E9 | Relay-urile văd muchia drop-ului (cluster care scrie → cluster care listează), ca la orice DM | livrare prin relay-uri | un singur blob de 1 KiB per invitat, la un moment tras la activare, cu credit sau fals; cheile de citire partajate (D11) reduc separarea cititorilor din Faza 9 |
| E10 | EAE și Janus la plata referral-ului în XMR | Monero | discountul e implicit și accesibil nu mai târziu decât plata (ambele cer credite de cel puțin 10 × valoarea unui credit); avertismente: subadresă proaspătă proprie, niciodată o adresă de exchange |
| E11 | Token-urile și creditele nu se recuperează din seed; după o restaurare, drop-urile invitațiilor create înainte nu mai sunt ascultate | valoarea la purtător e locală; `next_invite_index` nu se poate recupera | scanarea prevăzută de design (§8.4: după o restaurare, drop-urile 0..7 ascultate timp de 5 săptămâni, `restore_scan_until_day`) nu e implementată (L5), deci deocamdată orice credit trimis în drop-ul unei invitații create înainte de restaurare se pierde; după implementare se vor pierde doar cele din drop-urile cu index peste 7; anunț în UI (Faza 13); recuperarea din seed ca P1 (Q13) |
| E12 | Reorg după emitere; token-urile oneste ale epocilor revocate după compromiterea issuer-ului, inclusiv toate invitațiile și creditele nefolosite ale epocilor INVITE și CREDIT ale căror chei erau în memorie | token-urile blind nu se pot retrage sau reemite | contoare (`reorg_after_issue`); runbook I1; un RPC de schimb e lăsat Fazei 15 (design §18 F3) |
| E13 | Un relay confiscat împreună cu cheia sa leagă nullifier-e de namespace-uri cel mult două săptămâni (etichetele de legare) | răscumpărarea idempotentă cere legătura | nullifier-ele rămân nelegabile de plăți (blindness); nimic altceva nu se păstrează pe relay |
| E14 | Un client cu ceasul dispozitivului decalat cu peste 4 h primește `WRONG_PERIOD` la un apel care poartă `base_week` și își dezvăluie decalajul issuer-ului; relay-urile nu pot provoca asta, fiindcă timpul lor nu ajunge în deciziile către issuer | timpul pentru issuer trebuie să vină dintr-o sursă pe care operatorul nu o controlează | ceasul de încredere; toleranța de ±4 h a issuer-ului; nimic înregistrat la `WRONG_PERIOD`. Verificarea opțională față de consensul Tor (`CLOCK_UNTRUSTED`), care ar fi suprimat apelul, nu e implementată (Arti 0.46; L5) |
| E15 | Momentul plății (L6): portofelul operatorului vede plata în pool în câteva secunde sau minute; dacă GHOST are atunci o sesiune cu relay-urile, momentul plății selectează ≈ 1/32 dintre clustere (≈ 5 biți) și, cu L5, poate lega factura (și o plată KYC) de un cluster | utilizatorul plătește dintr-un portofel extern, adesea pe același telefon | `PAYMENT_READY` nu e niciodată o notificare imediată (primul prim-plan natural după U[1 h, 6 h]); ecranul de plată închide sesiunea cu relay-urile și nu permite alta U[20, 60] min după ultima afișare; după pornirea unui proces nou reținerea se trage din nou de la momentul salvat (niciodată sub 20 min), ceea ce favorizează ușor reținerile scurte la reporniri repetate; recomandarea de a plăti de pe alt dispozitiv sau mai târziu, fără GHOST deschis; modelat în lumea T2 (S3d; plăți de pe ecranul de plată, 70 %, și din timpul unei sesiuni cu relay-urile, 30 %) |
| E16 | Cumpărarea declanșată de epuizarea token-urilor (L7): relay-urile văd epuizarea, issuer-ul vede factura câteva ore mai târziu | declanșatorul e vizibil la relay-uri | `ENTITLEMENT_NEEDED` apare la un moment aleator în U[0, 12 h], iar `RequestInvoice` așteaptă încă U[0, 24 h]; token-urile noului pachet sunt folosibile doar de la un slot de activare; modelat în lumea T2 (cumpărări declanșate de nevoie, 5 % din N; design §19.24 punctul 6) |
| E17 | Reîmprospătarea unui credit primit e legabilă de invitatul care l-a trimis (sau de operatorul care se dă drept invitat): issuer-ul află un moment al unei rulări liniștite a celui care invită; două momente legate dacă prima încercare eșuează. Citirea creditului din drop, pe care relay-urile o văd, decide un singur bit: dacă a venit înaintea primului dintre cele două momente trase la crearea invitației (altfel reîmprospătarea vine la al doilea); asta se poate întâmpla doar pentru o citire din ultimele zile ale ascultării, iar relay-urile care țin un blob pe loc pot alege bitul, nimic altceva din momentul citirii | invitatul a finalizat creditul; reîmprospătarea vine după citire | apel singur, în rulare liniștită proprie și pe `IssuerFlow` propriu, la unul dintre două momente trase de client la crearea invitației (la 1–14 zile după ultima fereastră de drop a unui invitat, respectiv după sfârșitul ascultării, tăiate la fereastra de reîmprospătare a issuer-ului), niciodată la un moment stabilit de citire; cel mult o reîncercare; creditul nou e blind, deci cheltuielile și adresele de plată ulterioare rămân nelegate (Q31, design §19.26) |
| E18 | Mutarea de urgență a unui slot pe altă adresă onion pentru o săptămână deja publicată împarte clienții după versiunea ES | un relay pierdut nu se poate înlocui în versiunile vechi | doar după 48 h de indisponibilitate (Q24); sloturile, prețurile și cheile nu se schimbă (ADR-22) |
| E19 | Pe un dispozitiv confiscat (AD-8, AD-12), `eligible_minute` al unui token nefolosit arată ziua finalizării lotului lui, până la 5 săptămâni | eligibilitatea se verifică local | declarat în L1.1; rândurile terminale ale cumpărărilor nu păstrează nicio oră |
| E20 | Un relay care își pierde tot directorul de date (`relay.key`, marcajul, `nullifiers.redb`), dar își păstrează cheile onion, pornește ca un relay nou și ar accepta a doua răscumpărare a token-urilor deja răscumpărate în săptămânile deschise (cel mult două); cu `--nullifiers-reset`, token-urile săptămânilor refuzate la acel relay se pierd | relay-ul nu se poate deosebi singur de unul nou (Q25) | runbook O1: `--nullifiers-reset` după orice pierdere de date; `nullifiers.redb` nu se restaurează niciodată dintr-un backup vechi; replicarea acoperă calea datelor |
| E21 | O intrare de plată refuzată de stația operatorului (de exemplu o adresă folosită deja) închide revendicarea neplătită, iar creditele ei rămân cheltuite | nu există nicio cale de rambursare fără o identitate (Q26) | adresa se validează local și e refuzată la reutilizare (`ent_payout_used`); textul UI (Faza 13): subadresă proaspătă proprie |
| E22 | Un pachet plătit cu credite își pierde creditele dacă issuer-ul a înregistrat factura, dar ambele răspunsuri `RequestInvoice` s-au pierdut: creditele revin `CREDITS_SPENT` (MS-6 ține peste o singură reîncercare) | o a treia încercare ar depăși plafonul de 6 apeluri legate (E5) | reîncercarea identică; pierderea e vizibilă; alternativa (o coadă lentă derivată din sămânță, ca la `BlindSign`) ar restaura MS-6 cu prețul plafonului E5 (Q28: acceptat și declarat) |
| E23 | Cine are credite nefolosite destule pentru o revendicare (≥ 10) poate afla dacă o adresă pe care o cunoaște are o revendicare în așteptare (`ADDRESS_REJECTED`, nimic consumat) | o singură revendicare în așteptare per adresă, ca stația să nu plătească o adresă de două ori | adresa se șterge odată cu lotul plătit; recomandarea unei subadrese proaspete per revendicare |
| E24 | Un invitat care se oprește în mijlocul activării și nu reîncearcă timp de 8 zile își pierde invitația (o cerere repetată identic primește același trial cel puțin 8 zile, apoi `REPLAYED`) | cheile trialului se distrug ca orice cheie trecută (ADR-26 punctul 4) | activarea se reia la fiecare prim-plan, într-un proces nou (`ResumedInvite`) |
| E25 | O plată minată prima dată după `grace_height` (inclusiv una văzută în pool înainte) nu se creditează și se numără ca venit neatribuit | termenele sunt înălțimi fixe, fără rambursări | 24 h pentru plată plus 72 h de grație; textul de plată: „plătește cu mult înaintea termenului”; o tranzacție deja creditată rămâne la timp dacă e reminată până la `grace_height + 100` |
| E26 | Două pachete ale aceluiași client cumpărate apropiat sunt legabile la issuer prin timp | cantitățile sunt fixe; un utilizator greu cumpără al doilea pachet | un singur produs; rulări liniștite; declarat (design §4.4) |
| E27 | Invite-only nu e impus la issuer: un client modificat poate cumpăra fără invitație | o poartă de invitație la cumpărare ar lega invitația de factură | clientul oficial cere invitație sau genesis; fără invitație nu există nici trial, nici cel care invită; canalele rămân pe invitație prin MLS (Faza 10) (Q11) |
| E28 | Pierderea simultană a bazei issuer-ului (`issuer.redb`) și a jurnalului de emitere (`issued.journal`) pierde facturile plătite și neemise | restaurarea reface starea din instantaneu plus jurnal; fără amândouă nu mai rămâne nicio evidență a facturii | instantanee B1 (orare 48 h, zilnice 7 zile) pe disc criptat, plus jurnalul, scris cu fsync înainte de commit; restaurarea B1 (ADR-26 punctul 3; design §16 R13, §19.5) |
| E29 | Cota unei capabilități de scriere se poate depăși o dată per repornire a relay-ului | ledger-ul de cotă (`QuotaLedger`) stă în memorie, nu în `nullifiers.redb` | depășirea e mărginită de expirarea capabilității, aliniată la săptămână (`start(p + 1) + 1 h`): cel mult o cotă în plus per repornire, cât trăiește capabilitatea (ADR-25, Consecințe; design §10.4, Anexa C G4) |
| E30 | Durata reținerii pentru răscumpărare (Q29): o sesiune de fundal care a pornit cu o nevoie de scriere în așteptare rămâne deschisă după benzile ei până la primul pas al benzii de răscumpărare (READY + U[0, 30 s] plus pasul) sau până la termenul jobului; garda Tor și rețeaua locală văd o conexiune mai lungă, iar un relay al cărui circuit a rămas deschis îl vede închizându-se mai târziu. Se vede că la pornire aștepta o nevoie de scriere (lucru de trimis fără capabilitate, o capabilitate care expiră, epuizată sau refuzată), și când nu există niciun token eligibil și pasul nu răscumpără nimic | recuperarea în fundal a unui client ale cărui capabilități au expirat cere o sesiune care trăiește mai mult decât lucrul ei de sync | intrarea e doar numărul nevoilor de scriere la pornirea sesiunii, niciodată starea issuer-ului, numărul de token-uri sau o nevoie apărută mai târziu (R1); niciodată peste termenul jobului; se încheie imediat la prim-plan, la ecranul de plată (E15 neschimbat), la onStopJob sau la ștergere; nu se ține când transportul a eșuat; măsurat în lumea T2 și raportat (linia E30: sesiunile de fundal, cele armate, cele ținute după benzile lor, cu mediana, percentila 90 și maximul reținerii; design §12.6, §19.23 punctul 5, §19.26 punctul 2) |
| E31 | Un issuer fără tranziții (factură, emitere, invitație, credit, reîmprospătare, revendicare, lot) păstrează în `issued.journal` segmentul ultimei intrări, cu hash-urile de revendicare, nullifier-ele și adresele de plată din el, peste retenția de 7–14 zile, până la tranziția următoare | tăierea nu șterge niciodată segmentul care ține ultima intrare: repornirea și restaurarea continuă numerotarea de la ea (design §19.25) | cât issuer-ul face tranziții, o intrare trăiește cel mult 14 zile și o oră (`journal-prune.sh`, orar); declarat în `RUNBOOK.md` secțiunea 13; remediul propus (Q32: la schimbarea săptămânii issuer-ul decide o intrare-ancoră fără date într-un segment nou, iar segmentul vechi pleacă la `start(w + 2)`) vine în valul următor |

### L6.1 Seturi de anonimat la onboarding-ul fără invitație (genesis, E6)

Model: onboarding-urile genesis sosesc ca un proces Poisson cu λ pe zi, pe lângă cel considerat; cu sloturi zilnice, setul care împarte ziua slotului are în medie 1 + λ membri, iar un onboarding e singur în ziua lui cu probabilitatea e^−λ (R-privacy §5: λ = 2 → set 3, singur 13,5 %). High-privacy mode adaugă zile Geometric(1/2), deci prima folosire se împrăștie pe mai multe zile și setul crește.

| λ (onboarding-uri genesis pe zi) | set așteptat | singur în ziua lui |
|---|---|---|
| 1 | 2 | 36,8 % |
| 2 | 3 | 13,5 % |
| 5 | 6 | 0,67 % |
| 10 | 11 | 0,005 % |
| 20 | 21 | < 10⁻⁶ % |

---

## 5. Sinteză: ce rămâne cu adevărat imposibil după toate acestea

| Limită | După mitigări rămâne |
|---|---|
| Dispozitiv compromis | Atacatorul vede tot ce vede utilizatorul, cât timp are acces. Nu poate extrage chei hardware; nu vede conținutul istoricului expirat, dar vede hash-urile de deduplicare ale blob-urilor expirate recent (de regulă cel mult ~30 de zile după expirare; 111 zile pentru cele listate dar neaduse; L1.1), din statisticile de rețea și baterie ale sistemului, o cronologie a activității (volum per UID pe intervale de ~2 h, durata joburilor; L1.1), iar din entitlement token-urile nefolosite (valoare la purtător, cu ziua finalizării lotului lor, până la 5 săptămâni) și o cumpărare încă deschisă (L1.1, E19); pierde accesul după revocare + PCS |
| Operator cu issuer-ul și toate relay-urile (entitlement) | Nu poate lega o plată de un cluster de relay prin octeți, chei, prețuri sau perioade (ADR-22). Rămân momentele din scurgerea declarată L1–L7 (L6: prima folosire la un slot de activare, golurile rulărilor liniștite, momentul plății, cumpărarea după epuizare), pe care le mărginește testul T2 și, pentru ce permite L însăși, S4 ≤ 0,25 (lumea T2 livrată; rularea verde la scara gate încă neînregistrată, L5); din citirea unui credit primit din drop, reîmprospătarea lui arată cel mult un bit (E17, Q31) |
| GPA | Poate afla „acest IP folosește Tor, posibil GHOST” și un tipar temporal; momentele listărilor nu reflectă activitatea (T19), dar volumul și trimiterile o reflectă până la cover traffic (L2.1). Nu poate afla cu cine sau ce |
| Membru rău-intenționat | A văzut ce s-a postat cât a fost membru. Nu vede nimic după `Remove`; nu poate deanonimiza pe nimeni fără „reveal”; îl costă (strike-uri pe sponsor, abonament pierdut din canal) |
| ISP | Vede trafic către o destinație (bridge/VPN/Tor); cu WebTunnel arată ca HTTPS; vede și bootstrap-urile Tor periodice ale joburilor de fundal (L4, până la mitigările din Faza 12). Nu vede GHOST |

Formularea permisă în UI: „Reducem paguba și o facem vizibilă. Nu o putem face zero.”

---

## 6. ADR-urile rezultate (aprobate 2026-09-10)

- **ADR-13 — Guvernanță în canal: flag-uri, prag, carantină, strike pe sponsor, liste de excludere opt-in.** Implementare: politica canalului ca obiect MLS semnat; `Flag`, `Quarantine`, `Remove`, `Strike` ca mesaje/commit-uri MLS; ponderare după vechime și diversitatea sponsorilor; canale „cu verificare” pentru portabilitatea excluderii. Fază: 10 (MLS) pentru primitive, 13 (UX) pentru interfață. P1.
- **ADR-14 — Atestare hardware a cheilor și recuperare după compromitere.** Key Attestation în prekey bundle; certificat de revocare ca blob; re-verificare safety number; duress PIN (P2). Fază: 3 și 9. P1 (revocarea este P0 deja).
- **ADR-15 — Traffic shaping: polling constant, trimitere întârziată, bucketizare la citire.** Fază: 7 (sync) și 12 (hardening). P1, bucketizare P2.
- **ADR-16 — Rezistență la cenzură: bridges implicite în manifest, mod auto, WebTunnel/obfs4/Snowflake.** Fază: 6 (network) și 12. P1.

Fiecare ADR primește teste de acceptare în `test-harness/` înainte de a fi marcat „done”, conform DoD.
