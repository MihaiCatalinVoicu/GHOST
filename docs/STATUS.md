# GHOST — Status

**Stadiu: Developer preview. Nicio afirmație de confidențialitate.** (Spec v2.0 §3.1: „Local/test infrastructure; no privacy claim; synthetic payments.”)

| Element | Stare (2026-09-11) |
|---|---|
| Specificație | v2.0 FINAL (`output/pdf/GHOST_Technical_Specification_v2_FINAL.pdf`) + delta v2.1 (`docs/GHOST_Master_Plan_v2.1_OPTIMIZAT.md`) |
| ADR-uri | ADR-01 … ADR-19 **aprobate** (ADR-01 … ADR-18 pe 2026-09-10, ADR-19 pe 2026-09-11); ADR-20 **propus** 2026-09-11 și aplicat în Faza 7, în așteptarea deciziei proprietarului (`docs/adr/`) |
| Faza 0 (baseline, carantină, git) | **închisă** — legacy în `legacy/`, repo git, STATUS, ADR-uri |
| Faza 1 (monorepo `ghost/`, CI, gates) | **închisă** — build Android + Rust verde local; gates statice verzi și dovedite pe fixture-uri negative; schema proto validată; build reproductibil verificat local; CI în `.github/workflows/ci.yml` |
| Faza 2 (threat model v2.1, harness privacy) | **livrabile complete**: `docs/THREAT_MODEL_v2.1.md`, `ghost/test-harness/privacy/` (schema observabile, validator `capture-check`, T1 în CI); **gate deschis**: review independent al threat model-ului |
| Faza 3 (identitate și onboarding) | **nucleu livrat** în `ghost/android/identity`: entropie 256 bit, BIP-39 24 cuvinte (vectori Trezor), HKDF RFC 5869 (vectori RFC), ramuri separate, Ed25519 din seed (ADR-17), identitate `ghost1…` cu checksum, pseudonime per canal (T14), invitații semnate cu tamper/expiry/replay (T16), certificat de revocare, IdentityManager (create/restore/unlock/wipe/backup challenge), Keystore wrapper StrongBox→TEE, stocare no-backup. **Rămân**: test instrumentat pe dispozitiv pentru Keystore (emulator/device matrix), UI onboarding (Faza 13), redeem invite la issuer (Faza 8) |
| Faza 4 (storage SQLCipher) | **nucleu livrat** în `ghost/android/storage`: schema v1 (19 tabele din §8 + pseudonime, nonce-uri invitații, revocări; doar coloane-envelope, timpi la minut impuși de CHECK, FK/CASCADE, plafon 64 KiB pe payload), MigrationRunner tranzacțional cu recuperare după întrerupere și downgrade fail-closed, cheie DB învelită în Keystore, GhostDatabase peste SQLCipher (no-backup dir, cipher_memory_security, secure_delete), repository-uri pentru identitate, nonce-uri (replay persistent), pseudonime, revocări; 11 teste JVM pe SQLite. **Rămân**: test instrumentat pe dispozitiv (deschidere SQLCipher reală + T4 inspecție backup) |
| Faza 5 (relay v1 Rust + onion services) | **nucleu livrat** în `ghost/relay`: tipuri generate din proto (tonic), blob store redb (ADR-18) cu hash recalculat, bucket-uri impuse, TTL, cursoare opace, prune; capabilități HMAC locale cu cotă, nullifier-e; gossip de inventar (oprit implicit); nod gRPC `ghost-relay` cu mod captură; suita cu două noduri (abuz, quota, failover) + captura validată T1 pe trafic real; imagine Docker + torrc onion v3 + compose cu 3 relay-uri pentru staging. **Rămân**: deploy efectiv pe 3 operatori de staging, autentificare Noise între relay-uri și convergență de date (Faza 10), test de sarcină NFR-3 |
| Faza 6 (client Tor + network) | **nucleu livrat și revizuit** (review de securitate 2026-09-11: 6 dimensiuni, verificare adversarială; 27 constatări confirmate; remedierile au fost verificate adversarial într-o a doua rundă, care a găsit 11 regresii și lacune, rezolvate în runda 2; vezi `docs/reviews/2026-09-11-faza6-security-review.md`). `ghost/client-core/net` (Rust, ADR-19): Arti cu onion-service client, **vanguards-lite** și bridges simple (transporturile pluggable vin în Faza 12; liniile obfs4/webtunnel/snowflake sunt refuzate explicit); `OnionAddress` cu checksum v3 și vectori partajați Rust/Kotlin; API public onion-only (clientul Arti nu mai e expus); izolare de circuite per namespace (testul live compară circuitele reale a două scope-uri pe calea de conectare folosită și de clientul relay; aserțiunile noi (izolare pe circuite reale, keystore gol după bootstrap) au trecut pe Linux în jobul manual `live-tor`, rularea CI 34585120238 pe commit-ul cd145fc); client relay pe HTTP/2 fără `user-agent`, deadline 60 s pe RPC, bootstrap limitat la 180 s și anulabil, TTL rotunjit la bucket, validarea răspunsurilor unui relay rău-intenționat; blob-urile trebuie să fie deja criptate și de dimensiune bucket (padding-ul se face în plaintext-ul AEAD). JNI: registru de handle-uri cu anulare (fără use-after-free sau double-free), `catch_unwind` la graniță, categorii de eroare constante, documentate și testate. Relay: stocare pe apartenență (namespace, hash), schema v2 cu versiune (o bază din Faza 5 e refuzată), cota taxată în tranzacția de scriere, cursor per namespace. Build: `scripts/build-native.sh` (cargo-ndk și protoc fixate, căi de build remapate, verificare de scurgeri); CI construiește biblioteca de două ori în aceeași cale și compară hash-urile (determinism la aceeași cale; identitatea între medii vine cu builder-ul din Faza 14), iar APK-ul verificat de T12 conține exact aceste biblioteci; allowlist de pachete Rust pe ambele ABI, politică de feature-uri Arti/tonic (tonic doar `codegen`: fără `Channel` și fără server web în bibliotecă) și interdicții clippy pe API-uri clearnet numite, dovedite de un fixture negativ, ca gate-uri. Relay: expirări rotunjite la oră, iar rândul de secvență al unui namespace dispare odată cu ultimul lui blob. **Rămân**: T6 dinamic pe emulator (captură tcpdump cu Tor blocat), măsurători de latență NFR-1/2 pe dispozitive, bridges implicite și transporturi pluggable (Faza 12), generatorul de manifest care consumă `native-libs.sha256` (Faza 14), testul live pe Windows (bootstrap-ul stagnează local; verde pe Linux) |
| Faza 7 (sync engine) | **nucleu livrat; gate de ieșire deschis** (design aprobat prin „începe Faza 7”, 2026-09-11: `docs/design/faza7-sync-engine.md`, cu corecțiile normative §11). `ghost/android/storage`: schema v2 (9 tabele de sync, 12 triggere care impun mașinile de stare, gardă fail-closed la migrare, timpi persistați doar la minut/oră/zi impuși de CHECK, `retain_until_day` multiplu de 7 zile), `execUpdate`/`inTransaction`, tranzacții nereentrante în ambele executoare, `foreign_keys`/`secure_delete`/`synchronous = FULL` pe fiecare conexiune, verificate de `verifyIntegrity`; corectată o eroare din Faza 4 găsită în bytecode-ul sqlcipher-android (helper-ul marca versiunea înainte de migrare; migrarea rulează acum în callback-urile helper-ului; nevalidat încă pe SQLCipher real). Rust (completări ADR-19): `capability_header` definit o dată și folosit și de relay, `NamespaceClient` legat de namespace, cu verificarea scopului capabilității înainte de orice I/O (T21), JNI `check`, termen per apel, `get` cu expirarea relay-ului; relay: handler-e `*_at(now)` și vectori de conformitate `protocol/test-vectors/relay_semantics.txt` (8 secțiuni), reluați de relay-ul Rust și de modelul Kotlin. `ghost/android/sync` (nou, vezi `android/sync/README.md`): outbox idempotent (scriere pe toate relay-urile setului; `sent` numai când ≥ 2 operatori distincți arată copia în inventar; fereastră de stocare 7 zile; rezultate sent/degraded/failed/indeterminate), inbox cu deduplicare pe (namespace, hash) și cursoare opace per (relay, namespace), API pentru Fazele 9–11, motor pur JVM cu bandă de citire și bandă de lucru, program per pereche (ADR-15), backoff, breakere, bugete; Android: job JobScheduler + sesiune în prim-plan (ADR-20, propus), un singur transport Tor viu per proces, `GhostApp`, manifest (2 permisiuni noi, `ProfileInstallReceiver` eliminat), `lifecycle-process` declarat explicit. Gate-uri noi, cu fixture-uri: `sync-no-catch-all.sh`, `kotlin-clearnet.sh` (T6, partea Kotlin), `merged-manifest-lint.sh` (T15m, în jobul CI android); invarianți noi T19–T21. **Teste** (rulare locală fără cache, 2026-09-12): `:sync` 233, `:storage` 61, `:network` 9, `:app` 5, 0 eșecuri. Enumerarea exhaustivă (S-A…S-H și S-L, ambele moduri de jurnal) injectează 45 156 de crash-uri, toate ajunse la nivelul de sus și verificate după repornire, plus crash-uri duble în WAL pentru S-A, S-D și S-F; 1 000 de lumi cu seed (1 782 de crash-uri, 6,0 mil. de evenimente); mutanții M1–M17 detectați 17/17; separat, verificări de mutație pe cod: 8/8 (stores), 20/20 (motor), 17/17 (Android). Harness-ul a găsit un defect de producție (o sesiune de fundal putea să nu se mai termine), corectat cu test de regresie. **CI** pe commit-ul d4a2222 (S3–S6), rularea 34632710385: jobul `android` (inclusiv suita `:sync` implicită) și celelalte joburi automate verzi. În jobul `sync-exit-gate` (20 000 de seed-uri), eșecul de la seed-ul 18694 (liveness: o livrare confirmată fără un token cu care să poată fi verificată) a fost diagnosticat și corectat (`Capabilities.needed()` cere token de scriere oriunde outbox-ul mai are lucru la relay; test de regresie în `store/CapabilityTest`); local trec toate cele 20 000 de seed-uri (35 647 de crash-uri, 122,9 mil. de evenimente). Rezultatul CI al commit-ului de închidere îl consemnează lead-ul. **Review-ul adversarial S9**: 6 constatări confirmate, toate corectate; cele cinci de cod au fiecare un test care eșuează înainte și trece după, iar a șasea (statisticile OS) e o corectură de documentație (design §11.5): tombstone-uri proprii la pornirea ascultării, încrederea în ceas verificată la fiecare utilizare, cursor compare-and-set, verificarea IN-2 „consumatorul nu își vede propriile blob-uri” în harness, pagini în plus care nu mai țin perechea ocupată, statisticile OS de rețea și baterie declarate în LIMITE L1.1; plafonul cozii de liniștire a lumilor cu seed e derivat acum din H și TTL (seed-ul 18291). **Rămân**: verificările pe dispozitiv din design §8.9 (SQLCipher real: migrarea v1→v2, triggere, pragma-uri; legarea `SyncJobService` neexportat pe API 29 și 37; jobul persistent după reboot și force-stop; inventarul T20 și `getAllPendingJobs()` după wipe; Tor real în prim-plan și în fundal; cheie DB legată de autentificare; rularea live cu 3 relay-uri de staging și `am kill` repetat); măsurătorile NFR-1 (neîndeplinit prin design cu intervalul de 30 s, ADR-20 punctul 5) și NFR-5; deciziile proprietarului: ADR-20 și valorile implicite Q1–Q7 (tabelul din ADR-20); T7 pe classpath-ul rezolvat (F1, sarcină separată); observațiile harness-ului din LIMITE L5 |
| Faza 8 (entitlement issuer) | următoarea |
| Faze 9–18 | neîncepute |
| Cod de produs | schelet fără logică de produs; `legacy/` conține prototipuri simulate, în carantină |
| Gap cunoscut | commit-urile nu sunt încă semnate (FR-8.1): proprietarul trebuie să configureze cheia (`git config commit.gpgsign true`) |
| Afirmații permise în marketing/UI | niciuna (CP-10, FR-8.7) |

Regula de actualizare: acest fișier se modifică în același commit cu orice schimbare de fază sau de gate.
